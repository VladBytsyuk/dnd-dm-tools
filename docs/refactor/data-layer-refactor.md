# Source-Independent Data Layer Refactor

The original waterfall plan has been split into sequential, self-contained task documents under:

- [docs/refactor/data-layer/README.md](data-layer/README.md)

Start with the index, then execute each step in order. The split plan keeps the same target architecture but avoids changing services, mappers, stores, repositories, seed flow, and composition in one oversized pass.
